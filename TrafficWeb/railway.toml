[build]
builder = "NIXPACKS"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -Dserver.port=$PORT -Dspring.profiles.active=railway -jar target/TrafficSystem.jar"
healthcheckPath = "/api/stats"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
